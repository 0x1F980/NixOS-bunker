# Shared tap interface helper for app/net guests (imported where needed).
# mac/id must be unique per VM — set in each guest file.
{ lib, ... }:

{
  # Guests set microvm.interfaces themselves for clarity.
}
