[package]
name = "bunker-panic-tui"
version = "0.1.0"
edition = "2021"
description = "Panic button TUI — destroy panic-flagged deniable zone keys + RAM wipe"
license = "MIT"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"
