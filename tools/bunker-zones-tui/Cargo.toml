[package]
name = "bunker-zones-tui"
version = "0.1.0"
edition = "2021"
description = "Minimal ratatui UI for AppVM / Disposable CRUD (zones.json)"
license = "MIT"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
